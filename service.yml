apiVersion: v1
kind: Service

metadata:
  name: nginx-service
  namespace: jenkins

spec:
  type: NodePort

  selector:
    app: nginx

  ports:
    - port: 2000
      targetPort: 80
      nodePort: 30080
